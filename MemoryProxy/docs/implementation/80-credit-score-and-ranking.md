# 80 · S8：信用分与排序（credit-score-and-ranking）

> 状态：**S8-a（聚合层）已落地**；**S8-b（索引行 + 截断后精排）已落地**（开关 `attribution.ranking.enabled` 默认关 = 逐字节现状）。
> 基线：`feature/attribution-v2`。**本文件是 S8 的权威契约**（后续单在此 append，不改既有行）。

---

## 1 S8-a · 聚合层（D0 = (a) 纠正率 + 贝叶斯平滑）

### 1.1 公式与口径（写死）

- `credit = ((used − corrected) + k × baseline) / (used + k)`；缺省 `k = 5`（弱先验；显式传参可覆盖）。
- `used` / `corrected` = 按 `(asset_id, session_key)` **去重会话数**（同会话 5 次 used 只算 1 对，防水）——
  读侧复用 `rollupByAsset({ eventType })` 的 `session_count`（口径同 §3.5），**不改其语义、零 DDL、不物化**。
- `baseline` = **全团队加权** `(Σused − Σcorrected) / Σused`（**不是**"各资产平均的平均"）。

### 1.2 中性返回（不许凑数）

- `Σused = 0` ⇒ `baseline = null`；本资产 `used = 0` ⇒ `credit = baseline`（可能为 null）；
- **两者皆 null ⇒ `credit = null`**——调用方**不得**自行取 0.5（"凑一个看起来完整的公式"被明确禁止）。

### 1.3 可解释输出

`{ asset_id, credit, used, corrected, baseline }`——分子分母必须能看见（供 S8-b 与回执呈现）。

### 1.4 边界

- `corrected > used`（不变式破坏）⇒ **fail-closed：夹取 + 计数，不抛**（数据面异常不应炸调用方）；
- 排序辅助 `rankByCredit`（纯函数、**仅提供不接线**）：`credit = null` 视为"不参与"、原位不动。

---

## 2 语义收窄与幸存者偏差（**登记义务，不得省略**）

- **本口径回答的是"被采纳后有没有被推翻"，不是"历史上有多可靠"。**
- **幸存者偏差（如实登记）**：
  - "**从未被使用**"的资产：`used = 0` ⇒ 不产生 `credit`（中性 null），不会"冒充可信"；
  - "**被大量拒绝（`refuted`/`unconfirmed`）但偶用一次且未被纠正**"的资产：分母只有那 1 次 used ⇒
    credit 看似很高——**这是本口径的已知偏差**（根因：`refuted`/`unconfirmed` **不落行**、只累计
    进程内非持久计数，见 `status-events-repo.ts`）——因此本分**不得**被解读为"质量 / 可靠性"。

---

## 3 触发式登记（外部条件未到 ⇒ 不实施；同 L2/L3 处理方式）

| 项 | 触发判据 | 说明 |
|---|---|---|
| **(b) 补分母** | 需要 `refuted`/`unconfirmed` **落行**或**按资产计数** | 动已收口的 S5/S6 写侧 ⇒ 独立单评估 |
| **(c) outcome 化** | **`outcome` 覆盖率可测**（真库出现带 `outcome` 的 `asset_used` 行且能测覆盖率） | 届时才谈"验证通过率"与**提升度** |
| **提升度** | 同 (c)（需"验证通过率"） | **更正记录（本设计早期误记）**：曾写"提升度数据在（可先算）"——**该说法不严谨**：提升度需要"验证通过率"，而当前唯一可用信号 `outcome` 覆盖率未知（真库 `status_events = 0`）⇒ 提升度转为触发式（同 (c)） |
| **S8-d 效果验收** | 真库出现 `confirmed` 判定 **+ 必须包含"行级归属落地"**（见下行；否则会去验收一个 no-op） | 合成数据锁语义 ≠ 效果验收 |
| **`confirmed` 的可达性前提** | 引文**可达性**（`103` F8：(d) 粒度/方向**标定**） | 引文粒度 = **整条 piece** ⇒ 真实引用（模型回复里引用资产的一段话）结构上不可命中 ⇒ `confirmed` 近乎不可达；`104` 已清 block 自匹配**噪声**（正确性修复），但**可达性**属 (d)（须先标定，不得顺手改）。**(d)-1 已记录（`106` 影子度量 + 标定台）；(d)-2（切换判定 + 重标定）待标定后单独成单** |
| **方案 A（键口径接通的正路）** | 拍定"**行级资产身份**"（每条 L2 行挂独立 ref，`assetType='l2_scene'`）⇒ 事件面才有行级 `asset_id` | **跨切片**（牵动已收口的捕获面 / S0 / 候选分档）⇒ 挂起；具体形状须由真实效果数据倒逼（避免"边界没定先猜"） |
| **方案 B（段级口径）** | **明确否掉**（登记理由，防后人"顺手改成 `memoryAssetId`"） | 同一段每行渲染同一数值 = **误导性展示** |
| 拉取预算 / "必须拉全文"规则 / 池 DTO 进信用分 / 逐任务动态重算 | **各自触发式**（见本片 §4） | 防止"本该独立评估的架构决定被塞进另一个单子" |

> **语义声明（`110` · D6 落地；写清现状、零行为改动）**：`confirmed` **仅代表"整段逐字"级高置信引用**
> （**高精度、低召回**）；其余一律 `unconfirmed`，由 S8 的**中性值 / 保原序**承接。**不改阈值、不改真值表、
> 不改判定路径**。依据 = `103` F9/F10 + `109`（三条文本重合轴均不可分、A **无舒适档**）⇒ 文本重合
> **降格为筛选信号**（`suspect:text_overlap`，`70 spec §2.2`），**不得**被拔高成裁决信号。

> **适用范围与冻结边界（`114` · V3 冻结 + 边界声明；`113` 决策备忘分支 A）**——逐条写死：
> 1. **`confirmed` 语义**（`110` 立）：仅代表"**整段逐字**"级高置信引用（**高精度、低召回**）；
> 2. **S8 适用范围 = 段级 / `chat_memory` 类资产**；**L2 场景行须先做「方案 A（行级归属）」**，否则
>    **结构性**不可归因（键空间不同源，**不是缺数据**）；
> 3. **S8-c / S8-d 标「触发式」**：**S8-d** = **真实引用正例出现** ∧ **方案 A 落地**（否则验收对象是
>    no-op——与 `99` 的 S8-d 触发判据一致）；**S8-c** = **S8-d 通过后**（低优先）；
> 4. **开关状态**：`attribution.ranking.enabled` **默认 `false`**，**关时零行为回归**（已由 `98` 锁）；
> 5. **重新激活入口**（保持可回）：`方案 A brainstorm → 真实引用正例 → (d)-2 标定 → S8-d 验收 → 开开关`。
>
> **冻结 ≠ 删除**：S8-a/S8-b 的代码与文档**保留**、开关**保持默认关**——V3 属 roadmap `v3` 加分项
> （本次交付物 = 任务三 + 任务四，其核心基础设施 = V2（S4–S7））。

> **方案 A 的附带项（`99` 复核残项）**：`T7` 目前是"**键空间忠实**"而非**端到端取数路径**
> （`loadL2Ranking` **未导出**，测试无法经过它）⇒ **方案 A（或 S8-d）落地时必须同批**：导出该 loader
> 并补一格**真实库驱动**的端到端测试（= 随单条 `#6` 的**严格形态**）。
>
> **已提前补齐（`102`；2026-09-12）**：端到端测试已就位（临时库真链路：写真实生产者键事件 → repo →
> `rollupCreditsByAsset` → loader；①逐字节相同 + ②`creditByPath` 非空，两格并存）。
> **方案 A 落地时本测试必须同批改写**——它转红即提示"键口径已变"，属**预期信号而非噪声**。

> **方案 A 设计已定稿（`134` 拍定；2026-09-13）—— 解冻的是设计、不是实现**：
> **D1 = `A-ii`**（注入面携带稳定 ref + 事件面新列/新表承载；`creditByPath` 键口径改为该 ref）；
> **D2 = 不动 `F5`**（`asset_id` 仍**只放真实实体**）；**D3 = 注入面与查询面同批**（两半属同一改动）；
> **D4 = 实现先等 `(d)-2` 的初步结论**（两笔投入错开）；**D5 = L2 行暂不进候选**；**D6 = 登记**（见下条）。
> ⇒ 本表"方案 A"行的"挂起"自此**仅指实现**；定稿细则见 `134`（其"不空等"动作 = `(d)-2` 标定协议设计，见 `135`）。

> **`loadL2Ranking` 键不一致 ⇒ 静默空转（`134` · D6 登记；双触发）**：
> **项**：`creditByPath` **装** 事件面 `asset_id`（`tdai-profile-memory-injector.ts:143-145`）、**查** L2 行 `path`
> （同文件 `:208`；`correctedAtByPath` 同病）⇒ 除 `asset_id === path` 外**恒缺席**。
> **性质（不夸大）**：**"依赖（方案 A）未接通 ⇒ 静默空转"**，**不是逻辑错误** —— S8-b 按 `:133` 的假定写好，
> 而缺席的既定语义（不渲染该列 / 不参与精排）又把它**静默掩盖**（§4.1 是**外侧键空间**的 no-op 明证；
> 本条 = **同一事实的代码内侧**口径：**无论 A 走哪条路，这个键口径都必须跟着改**）。
> **双触发（取其先）**：① `attribution.ranking.enabled` 被打开；**或** ② 方案 A 实现单启动。
> **届时必修范围**：`creditByPath` **键口径** + **名字**（`ByPath` 名实不符）+ `correctedAtByPath` 键口径一致性（**与 `D3` 同批**）。
> **执行归属**：方案 A 实现单 / 开开关单（本档 = repo 侧登记；备查 = 章程 `随单条 #17`）。

> **`(d)-2` 标定协议（`135` 冻结；2026-09-13）—— 数据到齐 ⇒ 一次定案、不临场**：
> 1. **预注册决策表**（两种口径**同时**满足才采用；`X% = ≥ 80%`）：某判据 **FP = 0 ∧ FN = 0**（夹具口径）
>    **∧ 真实行保留率 ≥ 80%**（分布口径）⇒ **采用该判据**（`confirmed` 语义变更与 `(d)-2` 实现单同批）；
>    仅夹具口径满足 ⇒ **维持 C**，记入"**已证不可用**"清单；两口径都不满足 ⇒ **维持 C**，**回 `103 · D6` 重选维度**。
>    `80%` 的来处 = `F7` 现成参照（`L_q ≥ 32` 丢 38.5% / `≥ 48` 丢 52.0%，均被挡下）。
>    **不得**在数据到齐后改本表（改了即"事后凑阈值"）。
> 2. **正例路径**：**P-1 真实筛 / P-2 人工标注 ⇒ 可单独定案**；**P-3 合成 ⇒ 只能作辅助、不得单独定案**
>    （`109 → 110`：合成口径过 ≠ 可用）。
> 3. **维度可用性双口径**：夹具口径（**分层**报告 FP/FN，**不报总平均**）+ 真实行分布口径（保留率 ≥ 80%）。
> 4. **新维度同等待遇（`§3-b`）**：首批必验"**改写型反例是否也在该维度产生虚假匹配**"
>    （`F5`：HN3 已在引号跨度轴 `spans=2`，而该轴**唯一观测点恰在反例上**）；首个方向 = **引号/代码跨度**（`D4`）。
> 5. **规模**：`n ≥ 20` 且**分层**（沿用 `50 spec §13.3`：`{injected, fetched} × {正例, 反例}` 两轴张成、每类 ≥ 5）。
> 6. **与 L2 候选门（`D5`）关系**：只预留"L2 索引行文本面须单独出一组读数"一行，**不启用**。
> 7. **冻结位置**：本档 + 夹具顶层 `protocol`（`shadow-calibration-cases.json`）——**放顶层、不放 `expected`**：
>    `--record` 会**整替换** `expected`（脚本 `:553`），协议放里面会被下次 record 抹掉（如实登记的偏离及理由）。
> 8. **禁令再钉**：不得调阈值掩盖不可分；不得在标定前改真值表/阈值/判定路径；不得为凑 `confirmed` 放宽；
>    合成正例不得单独定案。

> **P-1 正例挖掘 + P-2 标注协议（`136`；设计 + 现库基线；2026-09-13）—— `(d)-2` 的数据前置动作**：
> **基线（复核：真库只读副本逐项验真）**：判定行 **10 / 有影子 5**（影子全 `round=1`）；候选 **52**（13+13+13+0+13）；
> **全局 `max(shadowBestContiguousRunChars) = 3`、`max(shadowQuotedSpanCount) = 0`、`max(shadowBestSegCoveragePerMsg) = 0`**
> （`9ec7` 的 13 行为 `unknown`——无资产文本，与 `121` 的 source 逐字自洽）；`run` 分布 = **`{0:13, 1:9, 2:27, 3:3}`**；
> `runNorm` max = `0.0526` ⇒ **现库候选数 = 0**（**强档 24 / 弱档 12 皆为 0**，距最低档约 **8 倍**）。
> ⇒ ① "真实引用正例 = 0"**不只是没人标**，而是**连候选门槛都没到**；② **`0` 只证明"尚未发生"、不证明"不可能"**
> （`109 → 110` 教训）。
> **副发现（补强上方协议块 §3-b；追加不改原句）**：引号/代码跨度轴在真实数据上 **`max = 0`**，而 `108 §⑦` 的 HN3
> （合成改写）**`spans = 2`** ⇒ 存在 **"全 0 型不可分"**（与"重叠型不可分"**并列的第二种失效**）
> ⇒ 新维度首批检验**加一问**："**真实数据上该轴是否有任何非零取值**"。
> **P-1 口径（`136 §2`；`D1`/`D2` 待拍）**：弱候选 `run ≥ 12`（宽松召回、**不得**直接当正例）／强候选 `run ≥ 24`
> （`109` 最低可用下界）；分层 = `50 spec §13.3` 骨架（`{injected, fetched} × {正例, 反例}` 每类 ≥ 5）——
> **挖掘只产候选、分类由 P-2 定**；**隐私纪律不放宽**：只元数据 + 指纹（`unit_id`/`session_key`/`assetId`/`run`/`norm`/
> `shadowBestSegSha256_16`）、**绝不落正文**；入口**只读真库 `.backup` 副本**（同标定台）；入口形态（建议）= 现有流水线
> 新模式（`scripts/qa/positive-mining.ts` 或标定台 `--mine`），输出候选清单 + **"落空报告"**（候选 0 时仍打印全局 max
> ⇒ **两种 0 可分**：真没有 vs 口径坏了）。
> **P-2 标注协议**：**AI 不得自标为正例**；二值问题（"该消息是否确实引用了该资产"）+ **不确定记 `unknown` 并排除**
> （不许"大概算"）；**`n ≥ 20` 且分层**（`135 · D3`）；标完冻结入夹具 `positives`/`hardNegatives` + 语料指纹
> （与顶层 `protocol` 绑定）。
> **明令（不得放宽）**：不落正文；合成样本**不得**充当 P-1/P-2 正例（`135 §3②`）；**不得**据"0 候选"下可达性结论；
> **不得**为凑候选放宽 `run` 门槛（= "不得调阈值掩盖"同源）。**本单不写脚本、不改判定**；**`D1–D4` 待拍**（`136 §5`；
> 其中 `D4` 建议 = 入口**与下一张实现单同批**落 repo）。

---

## 4 S8-b · 索引行 + 截断后精排（**已落地**）

**落点**：`src/injection/injectors/tdai-profile-memory-injector.ts`（L2 `<l2_scene_index>` 索引行——其头注原文即"索引行：路径 + summary"）+ 开关装配 `src/injection/index.ts`。

- **索引行（只加列）**：`asset_id` = 行内已有 `path`，**显式在列**；新列为**追加**形式（不改既有文本、不改块结构）：
  - 信用分：来自本片 §1 的 `rollupCreditsByAsset()`；`null`/缺席 ⇒ **不渲染该列**（不得渲染成 `0`）；
  - 标黄：来源 `asset_corrected`；渲染 = `` ⚠️ 可能已过期（检测时间: <该行 created_at ISO>） ``——
    **成对**给检测时间（60 spec §5 ②）、**不得**渲染为"当前版本"（①）、本行不显示任何 version
    ⇒ 天然不依赖 corrected payload 当"当前值"（③：需要当前版本时必须从 fetched 行侧重算）；
- **精排（截断之后）**：位置 = 内核给定的本轮候选集**之后**（本渲染不引入新截断）；**只改顺序、不改候选集**
  （单测以"排序前后集合相等"钉）；键 = 信用分降序；**同分 / null 保持原序**（复用 S8-a `rankByCredit`
  的稳定语义）；**不得**与相关性分数加权相加；
- **开关**：`attribution.ranking.enabled`（**默认 false**；只有显式布尔 `true` 生效）——
  **关 ⇒ 渲染走既有路径、逐字节现状**（`render-golden.test.ts` 字节级对照）、**零库访问**；
- **C5 触发式登记（本单不实现）**：拉取预算（每轮 ≤3）/ "回执要展示的标黄资产必须拉全文"触发规则 /
  逐任务动态重算 / 信用分进池 DTO——**各自触发式**（防止"本该独立评估的架构决定被塞进另一个单子"）。

### 4.1 诚实声明：当前键口径下开闸为 **no-op**（必读）

**事实**（三处代码证据）：① 块级 refs 挂给事件面的资产 = **整段 `memoryAssetId`**（`assetType: "chat_memory"`，
span 覆盖整个 `<agent>` 段——`tdai-profile-memory-injector.ts` 的 refs 构造）；② **全仓没有**把 scene
`path` 当 `asset_id` 落事件的路径（`bridge-fetch-assets.ts`：skill 用 requestId/responseId、memory 通道落
NULL）；③ S8-b 的**查/渲键 = `e.path`**（`loadL2Ranking` 的 `listByAsset(e.path…)` 与渲染列
`creditByPath.get(e.path)`）。
⇒ 这是**两个不同粒度、不同来源的键空间**（块级 `chat_memory-*` vs 行级 scene `path`）——**永不命中**。
**开闸后的实际表现**：信用列 / 标黄**不出现**、`rankByCredit` 全 `null` ⇒ **保持原序 = no-op**
（不报错、不错排——只是**什么都没读到**）。

**根因（两条，均登记）**：
- **(a) spec gap**：S8-b 契约只写"`asset_id` 必须显式在列"，**未指定键空间**（"path 即 assetId"是执行侧的理解）；
- **(b) 测试设计盲区**：原 T1/T2 **直接注入合成 `L2RankingData`**、从不经过 `loadL2Ranking` 的真实键空间
  ⇒ **绿而不证**（现由口径一致性测试补齐：见测试 `T7 键口径 no-op 明证`）。

**表述红线**：本功能**不得**被描述为"已接通 / 已生效"；正确表述 = "**已接线、当前离线（键口径不同源）**"。

---

## 5 边界声明

- **S8-a（聚合层）**：**零接线、无开关、零 DDL**（`SCHEMA_VERSION` 仍 1；`schema.ts` 零改动）——
  不改 injector / worker / 面板 / config 默认值；
- **S8-b（索引行 + 精排）**：**唯一接线面 = 注入排序开关**（默认关；关 = 逐字节现状）；不改相关性 /
  召回逻辑、不改候选集、不做逐任务动态重算（触发式，见 §4）。
