{
  "version": 1,
  "description": "58 标定标注集（N=25：四类 {injected,fetched}×{正例,反例} 每类 5 + DR-6a×1 + DR-6b×1组 + 边界×2；真链路 11 条 / 构造 14 条）",
  "cases": [
    {
      "id": "inj-pos-1",
      "category": "injected-正例",
      "provenance": "real-link",
      "description": "注入 skl-cal-alpha-1（block 原文命中、排他 0）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-alpha-1": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-alpha-1",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-cal-alpha-1",
        "rationale": "注入资产 block 原文 exact 命中、coverage 高、排他 0"
      }
    },
    {
      "id": "inj-pos-2",
      "category": "injected-正例",
      "provenance": "real-link",
      "description": "注入 skl-cal-alpha-2（block 原文命中、排他 0）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-alpha-2": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-alpha-2",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-cal-alpha-2",
        "rationale": "同上（第二独立样本）"
      }
    },
    {
      "id": "inj-neg-1",
      "category": "injected-反例",
      "provenance": "real-link",
      "description": "两资产共享同段 ⇒ 命中但排他 1 ⇒ refuted",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-b1": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ],
          "skl-cal-b2": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-b1",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        },
        {
          "assetId": "skl-cal-b2",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "refuted",
        "assetId": "skl-cal-b1",
        "rationale": "两资产共享同段 ⇒ 命中但排他 1（引用不排他）；最强 = 候选顺序第一 b1"
      }
    },
    {
      "id": "inj-neg-2",
      "category": "injected-反例",
      "provenance": "real-link",
      "description": "三资产共享同段 ⇒ 排他 2 ⇒ refuted",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-c1": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ],
          "skl-cal-c2": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ],
          "skl-cal-c3": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-c1",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        },
        {
          "assetId": "skl-cal-c2",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        },
        {
          "assetId": "skl-cal-c3",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "refuted",
        "assetId": "skl-cal-c1",
        "rationale": "三资产共享同段 ⇒ 排他 2；最强 = c1"
      }
    },
    {
      "id": "fet-pos-1",
      "category": "fetched-正例",
      "provenance": "real-link",
      "description": "双源 skl-cal-delta-1（抓+注 ⇒ fetched 优先 + 命中）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e2\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-delta-1": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-delta-1",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-cal-delta-1",
        "rationale": "双源（抓+注）⇒ fetched 优先候选 + 命中 + 排他 0"
      }
    },
    {
      "id": "fet-pos-2",
      "category": "fetched-正例",
      "provenance": "real-link",
      "description": "双源 skl-cal-delta-2（抓+注 ⇒ fetched 优先 + 命中）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e2\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-delta-2": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-delta-2",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-cal-delta-2",
        "rationale": "同上（第二独立样本）"
      }
    },
    {
      "id": "fet-neg-1",
      "category": "fetched-反例",
      "provenance": "real-link",
      "description": "只抓不注（无注入 proxy）⇒ 候选只有 fetched、无档①文本 ⇒ null ⇒ unconfirmed",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e2\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {},
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-g1",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        }
      ],
      "expected": {
        "verdict": "unconfirmed",
        "assetId": null,
        "rationale": "无注入 proxy：候选只有 fetched 且无档①文本 ⇒ null ⇒ unconfirmed"
      }
    },
    {
      "id": "fet-neg-2",
      "category": "fetched-反例",
      "provenance": "real-link",
      "description": "双源 h1 + 共享段 ⇒ 排他 1 ⇒ refuted",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e2\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-h1": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ],
          "skl-cal-h2": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-h1",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        },
        {
          "assetId": "skl-cal-h2",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "refuted",
        "assetId": "skl-cal-h1",
        "rationale": "双源 h1（fetched 优先）+ 与 h2 共享段 ⇒ 排他 1 ⇒ refuted @h1"
      }
    },
    {
      "id": "dr6a",
      "category": "DR-6a 版本漂移",
      "provenance": "real-link",
      "description": "注入 v1 → 库存改 v2 ⇒ 引文命中档①锚版 v1（按锚版判）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6a' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v1\n校准独特段落V1-ANCHOR：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6a' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v2\nV2-NEW-ANCHOR：升级后的全新文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"请按 校准独特段落V1-ANCHOR：本段是标定锚点文本 改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-v1": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v1\n校准独特段落V1-ANCHOR：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v2\nV2-NEW-ANCHOR：升级后的全新文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6a' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v1\n校准独特段落V1-ANCHOR：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v2\nV2-NEW-ANCHOR：升级后的全新文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "请按 校准独特段落V1-ANCHOR：本段是标定锚点文本 改 a.ts",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-v1",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-cal-v1",
        "rationale": "版本漂移：引文命中档①锚版 v1（cacheStrategy=session_init 使档①恒 v1），按锚版判 confirmed"
      }
    },
    {
      "id": "dr6b-anthropic",
      "category": "DR-6b 双通道",
      "provenance": "real-link",
      "description": "anthropic 形状（对照组 a）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6b-anthropic' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-dual — 双通道资产 v1\n校准独特段落DUAL-CH：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6b-anthropic' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-dual — 双通道资产 v1\n校准独特段落DUAL-CH：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-dual": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-dual — 双通道资产 v1\n校准独特段落DUAL-CH：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6a' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v1\n校准独特段落V1-ANCHOR：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v2\nV2-NEW-ANCHOR：升级后的全新文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6b-anthropic' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-dual — 双通道资产 v1\n校准独特段落DUAL-CH：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "请按 校准独特段落V1-ANCHOR：本段是标定锚点文本 改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-dual",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-cal-dual",
        "rationale": "双通道 a（anthropic）：与 b 同逻辑会话 ⇒ 判定须一致"
      }
    },
    {
      "id": "dr6b-openai",
      "category": "DR-6b 双通道",
      "provenance": "real-link",
      "description": "openai 形状（对照组 b）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6b-openai' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-dual — 双通道资产 v1\n校准独特段落DUAL-CH：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6b-openai' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>"
          },
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-dual — 双通道资产 v1\n校准独特段落DUAL-CH：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "\"改 a.ts\""
          },
          {
            "turnSeq": 1,
            "tier": "message",
            "content": "[{\"type\":\"tool_result\",\"tool_use_id\":\"e1\",\"content\":\"ok\"}]"
          }
        ],
        "assetTexts": {
          "skl-cal-dual": [
            "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-dual — 双通道资产 v1\n校准独特段落DUAL-CH：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task."
          ]
        },
        "corpus": [
          "<session_context>\n[Agent]\nid: agent-cal\nname: cal-agent\ndescription: d\nprompt:\np\n\n[Task]\nid: task-cal\nname: cal-task\ndescription: d\n</session_context>",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-1 — 校准资产ALPHA-1 v1\n校准独特段落ALPHA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-alpha-2 — 校准资产ALPHA-2 v1\n校准独特段落ALPHA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-b1 / skl-cal-b2 — 共享文档\n校准独特段落SHARED-B：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-inj-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-c1 / skl-cal-c2 / skl-cal-c3 — 共享文档\n校准独特段落SHARED-C：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-1' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-1 — 校准资产DELTA-1 v1\n校准独特段落DELTA-1：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-pos-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-delta-2 — 校准资产DELTA-2 v1\n校准独特段落DELTA-2：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-fet-neg-2' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-h1 / skl-cal-h2 — 共享文档\n校准独特段落SHARED-H：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6a' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v1\n校准独特段落V1-ANCHOR：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-v1 — 版本资产 v2\nV2-NEW-ANCHOR：升级后的全新文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6b-anthropic' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "## Skills (mandatory)\nBefore replying, scan the skills below. If a skill matches or is even partially relevant to your task, you MUST load it by calling the `skill_view` skill-bridge tool (see the `<skill_tools>` block above for the exact curl recipe) and follow its instructions. Err on the side of loading — it is always better to have context you don't need than to miss critical steps, pitfalls, or established workflows. Skills contain specialized knowledge — API endpoints, tool-specific commands, and proven workflows that outperform general-purpose approaches. Load the skill even if you think you could handle the task with basic tools like web_search or terminal. Skills also encode the user's preferred approach, conventions, and quality standards for tasks like code review, planning, and testing — load them even for tasks you already know how to do, because the skill defines how it should be done here.\nIf a skill has issues, fix it with the `skill_patch` skill-bridge tool.\nAfter difficult/iterative tasks, offer to save the approach as a new skill (`skill_create`). If a skill you loaded was missing steps, had wrong commands, or needed pitfalls you discovered, update it before finishing.\n\n以下是你（当前 agent）自带的云端 skill 列表。这些 skill 存储在你的 agent 名下，\n优先使用它们完成任务。如果你觉得自带的 skill 不够，可以用 skill_search 工具\n在团队的 skill 库中检索更多（跨 agent 共享）。\n\n**重要：这些 skill 存储在云端，不能使用 read_file / tool_use 直接访问，\n必须用 Bash 执行 curl 调用上方 <skill_tools> 块中的 skill-bridge 工具。**\n\n## Skills (mandatory)\n- skl-cal-dual — 双通道资产 v1\n校准独特段落DUAL-CH：本段是标定锚点文本\n\nOnly proceed without loading a skill if genuinely none are relevant to the task.",
          "<skill_tools>\n以下是云端 skill 操作工具。**这些不是本地工具**，需要用 Bash 调用 curl 命中 proxy 的 skill-bridge 路径来执行。\nproxy 会自动注入身份与鉴权（user_id / team_id / agent_id 由 session 决定），body 里你只需要传业务字段。\n\n调用模板：\n  curl -sSk -X POST <bridge>/<action> -H 'content-type: application/json' -H 'x-tdai-service-id: sp-cal' -H 'x-conversation-id: cal-dr6b-openai' -d '{...业务字段...}'\n  其中 <bridge> = http://10.163.27.109:0/skill-bridge/v3/skill\n\n可用工具：\n\n  <tool name=\"skill_search\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/search\n    body: {\"query\": \"描述你要找什么 skill 的关键词（必填，>=1字符）\"}\n    use:  在**你在团队中有权限访问**的 skill 中按关键词 + 语义检索匹配项（跨 agent，但**不含**其他人设置为私密的 skill —— 与前端「团队资产」tab 展示一致）。query 必须是非空字符串，建议写 2-5 个相关关键词。当你觉得自己自带的 skill 不够用时，用它发现团队里其他可用的 skill。返回条数由服务端固定，若结果不理想请换一组关键词重试，不要在 body 里加 top_k/mode 等其它字段（会被忽略）。\n  </tool>\n\n  <tool name=\"skill_view\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/get\n    body: {\"skill_id\": \"<skill 的 id, 形如 skl-xxx>\", \"include_content\": true, \"include_manifest\": true}\n    use:  **打开一个 skill 的入口**：拿到 SKILL.md 全文 + 资源目录树（manifest）。想读某个资源文件的字节，必须先调这个工具从 manifest 里挑出 path，再用 skill_files_read。skill_id 取 <available_skills> 里每行的 `id=` 字段值（形如 skl-xxxxxx），或 skill_search 结果里的 skill_id 字段。\n  </tool>\n\n  <tool name=\"skill_files_read\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/files/read\n    body: {\"skill_id\": \"skl-xxx\", \"path\": \"scripts/run.sh\", \"encoding\": \"utf-8|base64\"}\n    use:  读取单个资源文件内容。**必须先调 skill_view 拿 manifest**，从里面挑出 skill_id + path，本工具才能定位。默认返回 JSON 信封（含 base64/utf-8 编码的字节）。\n    若需下载到本地：在 curl 末尾加 -o <本地路径>，proxy 会返回原始字节直接写入文件，不进上下文。下载的脚本需 chmod +x 后再执行。\n  </tool>\n\n  <tool name=\"skill_extract\">\n    path: http://10.163.27.109:0/skill-bridge/v3/skill/extract\n    body: {\"reason\": \"?可选，简要说明为什么觉得当前对话值得提取为 skill（写清楚有助于后台抽取器识别边界）\"}\n    use:  立即归档当前对话触发一次 skill 抽取（异步任务，由后台 agent 分析对话内容生成 skill）。proxy 从 session 拿身份 + 用 core 侧累积的对话缓冲，你不用传 messages。适合在\"用户已经跑通一段完整流程、值得复用\"时主动触发。\n  </tool>\n\n注意：当前仅开放只读操作。如需创建/修改 skill 请联系管理员。\n错误处理：响应是 `{code, message, request_id, data?}` 信封；`code != 0` 表示业务错。常见：\n- 40001 参数校验失败：body 字段缺失/格式错，看 message 里具体字段名。\n- 40101 session not initialized：session 未识别（很可能你在错误的 conversation 环境用了这个工具）。\n- 40401 SKILL_NOT_FOUND：skill 不存在或不属于你所在的 agent；先用 skill_search 找同类 skill。\n- 50301 upstream unavailable：core 侧临时不可达，稍后重试。\n</skill_tools>",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "[object Object]",
          "请按 校准独特段落V1-ANCHOR：本段是标定锚点文本 改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]",
          "改 a.ts",
          "[object Object]"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-cal-dual",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-cal-dual",
        "rationale": "双通道 b（openai）：与 a 逐条一致"
      }
    },
    {
      "id": "fake-inj-pos-fp3",
      "category": "injected-正例",
      "provenance": "constructed",
      "description": "构造：block 引文 = 资产全文 5/5 句（coverage≈1）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产fp3内容。第二句资产fp3内容。第三句资产fp3内容。第四句资产fp3内容。第五句资产fp3内容。"
          }
        ],
        "assetTexts": {
          "skl-fp3": [
            "第一句资产fp3内容。第二句资产fp3内容。第三句资产fp3内容。第四句资产fp3内容。第五句资产fp3内容。"
          ]
        },
        "corpus": [
          "第一句资产其它甲内容。第二句资产其它甲内容。第三句资产其它甲内容。",
          "第一句资产其它乙内容。第二句资产其它乙内容。第三句资产其它乙内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-fp3",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-fp3",
        "rationale": "coverage=1.0"
      }
    },
    {
      "id": "fake-inj-pos-fp4",
      "category": "injected-正例",
      "provenance": "constructed",
      "description": "构造：block 引文 = 资产全文 4/5 句（coverage≈0.8）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产fp4内容。第二句资产fp4内容。第三句资产fp4内容。第四句资产fp4内容。"
          }
        ],
        "assetTexts": {
          "skl-fp4": [
            "第一句资产fp4内容。第二句资产fp4内容。第三句资产fp4内容。第四句资产fp4内容。第五句资产fp4内容。"
          ]
        },
        "corpus": [
          "第一句资产其它甲内容。第二句资产其它甲内容。第三句资产其它甲内容。",
          "第一句资产其它乙内容。第二句资产其它乙内容。第三句资产其它乙内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-fp4",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-fp4",
        "rationale": "coverage≈0.8"
      }
    },
    {
      "id": "fake-inj-pos-fp5",
      "category": "injected-正例",
      "provenance": "constructed",
      "description": "构造：block 引文 = 资产全文 3/5 句（coverage≈0.6）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产fp5内容。第二句资产fp5内容。第三句资产fp5内容。"
          }
        ],
        "assetTexts": {
          "skl-fp5": [
            "第一句资产fp5内容。第二句资产fp5内容。第三句资产fp5内容。第四句资产fp5内容。第五句资产fp5内容。"
          ]
        },
        "corpus": [
          "第一句资产其它甲内容。第二句资产其它甲内容。第三句资产其它甲内容。",
          "第一句资产其它乙内容。第二句资产其它乙内容。第三句资产其它乙内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-fp5",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-fp5",
        "rationale": "coverage≈0.6"
      }
    },
    {
      "id": "fake-inj-neg-1",
      "category": "injected-反例",
      "provenance": "constructed",
      "description": "构造：两资产共享段落 ⇒ 命中但排他 1 ⇒ refuted",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "共享段落SIGMA：这段文字同时写在两个资产的文档里。"
          }
        ],
        "assetTexts": {
          "skl-fn-a": [
            "共享段落SIGMA：这段文字同时写在两个资产的文档里。"
          ],
          "skl-fn-b": [
            "前言。共享段落SIGMA：这段文字同时写在两个资产的文档里。"
          ]
        },
        "corpus": [
          "第一句资产其它丙内容。第二句资产其它丙内容。第三句资产其它丙内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-fn-a",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        },
        {
          "assetId": "skl-fn-b",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "refuted",
        "assetId": "skl-fn-a",
        "rationale": "排他 1；最强 = 顺序第一 skl-fn-a"
      }
    },
    {
      "id": "fake-inj-neg-2",
      "category": "injected-反例",
      "provenance": "constructed",
      "description": "构造：引文只覆盖资产 3/10 句 ⇒ coverage≈0.3 ⇒ unconfirmed（T_COV>0.3 时）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产fn2内容。第二句资产fn2内容。第三句资产fn2内容。"
          }
        ],
        "assetTexts": {
          "skl-fn2": [
            "第一句资产fn2内容。第二句资产fn2内容。第三句资产fn2内容。第四句资产fn2内容。第五句资产fn2内容。第六句资产fn2内容。第七句资产fn2内容。第八句资产fn2内容。第九句资产fn2内容。第十句资产fn2内容。"
          ]
        },
        "corpus": [
          "第一句资产其它丁内容。第二句资产其它丁内容。第三句资产其它丁内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-fn2",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "unconfirmed",
        "assetId": null,
        "rationale": "coverage≈0.3（低于任何 ≥0.4 的 T_COV）"
      }
    },
    {
      "id": "fake-inj-neg-3",
      "category": "injected-反例",
      "provenance": "constructed",
      "description": "构造：引文与资产无关 ⇒ none ⇒ unconfirmed",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "与任何资产都无关的一段话。"
          }
        ],
        "assetTexts": {
          "skl-fn3": [
            "第一句资产fn3内容。第二句资产fn3内容。第三句资产fn3内容。第四句资产fn3内容。第五句资产fn3内容。"
          ]
        },
        "corpus": [
          "第一句资产其它戊内容。第二句资产其它戊内容。第三句资产其它戊内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-fn3",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "unconfirmed",
        "assetId": null,
        "rationale": "matchLevel=none"
      }
    },
    {
      "id": "fake-fet-pos-ff1",
      "category": "fetched-正例",
      "provenance": "constructed",
      "description": "构造（fetched 候选）：block 引文 = 资产全文 5/5 句",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产ff1内容。第二句资产ff1内容。第三句资产ff1内容。第四句资产ff1内容。第五句资产ff1内容。"
          }
        ],
        "assetTexts": {
          "skl-ff1": [
            "第一句资产ff1内容。第二句资产ff1内容。第三句资产ff1内容。第四句资产ff1内容。第五句资产ff1内容。"
          ]
        },
        "corpus": [
          "第一句资产其它己内容。第二句资产其它己内容。第三句资产其它己内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-ff1",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-ff1",
        "rationale": "coverage=1.0"
      }
    },
    {
      "id": "fake-fet-pos-ff2",
      "category": "fetched-正例",
      "provenance": "constructed",
      "description": "构造（fetched 候选）：block 引文 = 资产全文 4/5 句",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产ff2内容。第二句资产ff2内容。第三句资产ff2内容。第四句资产ff2内容。"
          }
        ],
        "assetTexts": {
          "skl-ff2": [
            "第一句资产ff2内容。第二句资产ff2内容。第三句资产ff2内容。第四句资产ff2内容。第五句资产ff2内容。"
          ]
        },
        "corpus": [
          "第一句资产其它己内容。第二句资产其它己内容。第三句资产其它己内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-ff2",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-ff2",
        "rationale": "coverage≈0.8"
      }
    },
    {
      "id": "fake-fet-pos-ff3",
      "category": "fetched-正例",
      "provenance": "constructed",
      "description": "构造（fetched 候选）：block 引文 = 资产全文 2/4 句",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产ff3内容。第二句资产ff3内容。"
          }
        ],
        "assetTexts": {
          "skl-ff3": [
            "第一句资产ff3内容。第二句资产ff3内容。第三句资产ff3内容。第四句资产ff3内容。"
          ]
        },
        "corpus": [
          "第一句资产其它己内容。第二句资产其它己内容。第三句资产其它己内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-ff3",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        }
      ],
      "expected": {
        "verdict": "confirmed",
        "assetId": "skl-ff3",
        "rationale": "coverage=0.5（边界格：T_COV≤0.5 才 confirmed）"
      }
    },
    {
      "id": "fake-fet-neg-1",
      "category": "fetched-反例",
      "provenance": "constructed",
      "description": "构造（fetched 候选）：两资产共享段 ⇒ 排他 1 ⇒ refuted",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "共享段落SIGMA：这段文字同时写在两个资产的文档里。"
          }
        ],
        "assetTexts": {
          "skl-ff-a": [
            "共享段落SIGMA：这段文字同时写在两个资产的文档里。"
          ],
          "skl-ff-b": [
            "共享段落SIGMA：这段文字同时写在两个资产的文档里。"
          ]
        },
        "corpus": [
          "第一句资产其它庚内容。第二句资产其它庚内容。第三句资产其它庚内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-ff-a",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        },
        {
          "assetId": "skl-ff-b",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "refuted",
        "assetId": "skl-ff-a",
        "rationale": "排他 1；最强 = 顺序第一 skl-ff-a"
      }
    },
    {
      "id": "fake-fet-neg-2",
      "category": "fetched-反例",
      "provenance": "constructed",
      "description": "构造：fetched 候选无资产文本 ⇒ null ⇒ unconfirmed",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产fnf2内容。第二句资产fnf2内容。第三句资产fnf2内容。"
          }
        ],
        "assetTexts": {},
        "corpus": [
          "第一句资产其它辛内容。第二句资产其它辛内容。第三句资产其它辛内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-fnf2",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        }
      ],
      "expected": {
        "verdict": "unconfirmed",
        "assetId": null,
        "rationale": "matchLevel=null（无文本）"
      }
    },
    {
      "id": "fake-fet-neg-3",
      "category": "fetched-反例",
      "provenance": "constructed",
      "description": "构造（边界）：空语料 ⇒ coverage=unknown ⇒ unconfirmed（不落 0/1）",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": "第一句资产fnf3内容。第二句资产fnf3内容。第三句资产fnf3内容。"
          }
        ],
        "assetTexts": {
          "skl-fnf3": [
            "第一句资产fnf3内容。第二句资产fnf3内容。第三句资产fnf3内容。第四句资产fnf3内容。第五句资产fnf3内容。"
          ]
        },
        "corpus": []
      },
      "candidates": [
        {
          "assetId": "skl-fnf3",
          "assetType": "skill",
          "evidenceSourceType": "fetched"
        }
      ],
      "expected": {
        "verdict": "unconfirmed",
        "assetId": null,
        "rationale": "coverage=unknown（空语料，不落 0/1）"
      }
    },
    {
      "id": "fake-edge-1",
      "category": "边界",
      "provenance": "constructed",
      "description": "边界：candidates.length===0 ⇒ 显式 unconfirmed（B5：不得静默 false）",
      "source": {
        "pieces": [],
        "assetTexts": {},
        "corpus": [
          "第一句资产其它壬内容。第二句资产其它壬内容。"
        ]
      },
      "candidates": [],
      "expected": {
        "verdict": "unconfirmed",
        "assetId": null,
        "rationale": "candidates=[] ⇒ 显式 unconfirmed"
      }
    },
    {
      "id": "fake-edge-2",
      "category": "边界",
      "provenance": "constructed",
      "description": "边界：matchLevel=none（有资产文本但引文不命中）⇒ unconfirmed",
      "source": {
        "pieces": [
          {
            "turnSeq": 1,
            "tier": "block",
            "content": " completely unrelated ascii text "
          }
        ],
        "assetTexts": {
          "skl-fe2": [
            "第一句资产fe2内容。第二句资产fe2内容。第三句资产fe2内容。第四句资产fe2内容。第五句资产fe2内容。"
          ]
        },
        "corpus": [
          "第一句资产其它癸内容。第二句资产其它癸内容。第三句资产其它癸内容。"
        ]
      },
      "candidates": [
        {
          "assetId": "skl-fe2",
          "assetType": "skill",
          "evidenceSourceType": "injected"
        }
      ],
      "expected": {
        "verdict": "unconfirmed",
        "assetId": null,
        "rationale": "matchLevel=none"
      }
    }
  ]
}
